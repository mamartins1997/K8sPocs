apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: todowebapi
  labels:
    app: todowebapi
spec:
  selector:
    matchLabels:
      app: todowebapi
  replicas: 2
  template:
    metadata: 
      labels:
        app: todowebapi
    spec:
      containers:
        - name: todowebapi
          image: mamartins1997/todowebapi:latest